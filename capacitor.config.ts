import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appName: 'Perpustakaan Test',
  webDir: 'www',
  appId: 'io.ionic.starter',
  bundledWebRuntime: false
};

export default config;
